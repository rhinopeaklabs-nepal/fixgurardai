# Pins verified against Python 3.14 (local dev) and 3.12 (Docker/VPS).
fastapi>=0.141,<1.0
uvicorn>=0.52,<1.0
pydantic>=2.13,<3.0

# Must stay on the same minor as the Playwright base image tag in the
# Dockerfile (v1.62.0-noble). The image bakes in one Chromium build; a
# newer client library looks for a browser revision that was never
# downloaded, and every audit fails at launch with "Executable doesn't
# exist at /ms-playwright/chromium-<rev>". Bump both together or neither.
playwright>=1.62,<1.63
